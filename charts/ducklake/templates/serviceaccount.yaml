{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ducklake.serviceAccountName" . }}
  labels:
    {{- include "ducklake.labels" . | nindent 4 }}
{{- end }}
